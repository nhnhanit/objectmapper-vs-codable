# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'ObjectMapper-vs-Codable' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ObjectMapper-vs-Codable
  pod 'ObjectMapper'
end

target 'ObjectMapper-vs-CodableTests' do
    inherit! :search_paths
    # Pods for testing
end

target 'ObjectMapper-vs-CodableUITests' do
    # Pods for testing
end
